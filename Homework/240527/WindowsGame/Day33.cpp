#include "pch.h"

void Comment1()
{
	// 1. UI Button
	// 함수포인터 사용

	// 함수포인터라는 단어 처음들음

	// 1. 함수포인터 => 콜백
	// 오늘 여기까지 했는데 사실상 여기까지가 우리가 배울 C++문법의 모든 것

	// 세부적인게 더 많이 있지만 일단 이정도 알아두면 입사할 수 있다.
	
	// 2. Actor Layer (소환된 순서로 나온다. 즉, 나중에 소환된애가 항상 맨앞에 나온다)
	//  (뒤에 나와야하는 배경 엑터를 소환했는데, 플레이어가 가려지면 안된다.)
}

void Comment2()
{
	// 숙제
	// 그 데브1씬 실행했을때 가운데 아이콘 클릭할 수 있는거
	// 그거 클릭하면 데브2씬으로 넘어가도록 만들어보기
	// 오늘 수업 마지막부분만 활용해보면 된다.
}